# Backend environment example

# Local-only: bypass auth for development/testing (DO NOT use in prod)
DISABLE_AUTH=1

# Required for real JWT validation (set any non-empty string for local if needed)
SUPABASE_JWT_SECRET=changeme

# Optional environment indicator
APP_ENV=local

